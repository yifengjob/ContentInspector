# 自定义敏感词逻辑搜索功能实施方案

**创建日期**: 2026-05-12  
**版本**: v1.0  
**分支**: `feature/custom-sensitive-expression`  
**状态**: 待实施

---

## 🎯 实施目标

实现自定义敏感词逻辑搜索功能，支持用户使用逻辑表达式（`!`、`&`、`|`、`()`）进行敏感词匹配。

---

## 📋 实施原则

1. **模块职责单一**：每个模块只负责一个明确的职责
2. **依赖关系清晰**：模块间依赖明确，避免循环依赖
3. **类型安全**：无 TypeScript 类型问题
4. **日志规范**：使用已实现的日志架构（mainLogger）
5. **文档规范**：操作性强，可对照文档逐步实施
6. **内存安全**：无内存泄漏风险

---

## 🗂️ 实施步骤总览

### Phase 1: 基础架构（预计 2-3 小时）

1. ✅ 安装依赖库（expr-eval）
2. ✅ 创建类型定义文件
3. ✅ 实现表达式解析器封装
4. ✅ 编写单元测试

### Phase 2: 后端集成（预计 3-4 小时）

5. ✅ 扩展配置管理（AppConfig）
6. ✅ 修改敏感数据检测函数
7. ✅ 集成到 Worker 线程
8. ✅ 添加 IPC handlers

### Phase 3: 前端集成（预计 2-3 小时）

9. ✅ 添加工具栏输入框 UI
10. ✅ 实现实时验证和防抖
11. ✅ 暴露 Preload API
12. ✅ 添加 TypeScript 类型声明

### Phase 4: 测试和优化（预计 2-3 小时）

13. ✅ 端到端测试
14. ✅ 性能优化（缓存、短路求值）
15. ✅ 错误处理完善
16. ✅ 文档更新

---

## 🔧 详细实施步骤

### Step 1: 安装依赖库

**操作**：
```bash
cd /Users/yifeng/数据/开发/项目/ElectronProjects/DataGuardScanner
pnpm add expr-eval
```

**验证**：
```bash
# 检查 package.json 中是否添加了依赖
grep "expr-eval" package.json
```

**预期结果**：
```json
{
  "dependencies": {
    "expr-eval": "^2.x.x"
  }
}
```

---

### Step 2: 创建类型定义文件

**文件路径**: `src/types/expression.ts`

**职责**：定义表达式解析相关的 TypeScript 类型

**内容**：
```typescript
/**
 * 表达式验证结果
 */
export interface ExpressionValidationResult {
  valid: boolean;
  error?: string;
  position?: number;  // 错误位置（字符索引）
}

/**
 * 表达式评估上下文
 */
export interface ExpressionContext {
  text: string;                    // 待检测文本
  keywords: Record<string, boolean>;  // 关键词匹配结果
}

/**
 * 表达式评估结果
 */
export interface ExpressionEvaluationResult {
  matched: boolean;
  error?: string;
}
```

**注意事项**：
- ✅ 类型定义清晰，易于理解
- ✅ 不包含业务逻辑，纯类型定义
- ✅ 导出所有需要的类型

---

### Step 3: 实现表达式解析器封装

**文件路径**: `src/utils/expression-parser.ts`

**职责**：
- 封装 expr-eval 库
- 提供表达式转换功能
- 提供验证和评估接口

**核心功能**：

#### 3.1 导入依赖
```typescript
import { Parser } from 'expr-eval';
import { mainLogger } from '../logger/logger';
import { 
  ExpressionValidationResult, 
  ExpressionContext,
  ExpressionEvaluationResult 
} from '../types/expression';
```

#### 3.2 创建 Parser 实例（单例）
```typescript
// 单例 Parser 实例，避免重复创建
const parser = new Parser();

// 配置自定义函数
parser.functions = {
  /**
   * 检查文本是否包含关键词
   * @param text 文本内容
   * @param keyword 关键词
   */
  contains: (text: string, keyword: string): boolean => {
    return text.includes(keyword);
  }
};
```

#### 3.3 表达式转换函数
```typescript
/**
 * 将用户输入的表达式转换为 expr-eval 支持的格式
 * 
 * 转换规则：
 * - 关键词 "密码" → contains(text, '密码')
 * - 运算符 "&" → "&&"
 * - 运算符 "|" → "||"
 * - 运算符 "!" → "!"
 * 
 * 示例：
 * 输入: "密码 & (身份证 | 银行卡)"
 * 输出: "contains(text, '密码') && (contains(text, '身份证') || contains(text, '银行卡'))"
 * 
 * @param expression 用户输入的表达式
 * @returns 转换后的表达式
 */
function transformExpression(expression: string): string {
  // 1. Tokenize：分解为 token 数组
  const tokens = tokenize(expression);
  
  // 2. 转换每个 token
  const transformedTokens = tokens.map(token => {
    if (isKeyword(token)) {
      // 关键词转换为 contains 函数调用
      return `contains(text, '${escapeString(token)}')`;
    } else if (token === '&') {
      return '&&';
    } else if (token === '|') {
      return '||';
    } else {
      // 括号和 ! 保持不变
      return token;
    }
  });
  
  // 3. 拼接为最终表达式
  return transformedTokens.join(' ');
}

/**
 * 词法分析：将表达式字符串分解为 token 数组
 * @param expression 表达式字符串
 * @returns token 数组
 */
function tokenize(expression: string): string[] {
  const tokens: string[] = [];
  let currentToken = '';
  
  for (let i = 0; i < expression.length; i++) {
    const char = expression[i];
    
    if (char === ' ') {
      // 空格作为分隔符
      if (currentToken) {
        tokens.push(currentToken);
        currentToken = '';
      }
    } else if (['(', ')', '!', '&', '|'].includes(char)) {
      // 运算符和括号单独作为 token
      if (currentToken) {
        tokens.push(currentToken);
        currentToken = '';
      }
      tokens.push(char);
    } else {
      // 其他字符累积为关键词
      currentToken += char;
    }
  }
  
  // 处理最后一个 token
  if (currentToken) {
    tokens.push(currentToken);
  }
  
  return tokens;
}

/**
 * 判断 token 是否为关键词
 */
function isKeyword(token: string): boolean {
  return !['(', ')', '!', '&', '|'].includes(token);
}

/**
 * 转义字符串中的特殊字符（防止注入）
 */
function escapeString(str: string): string {
  return str.replace(/'/g, "\\'");
}
```

#### 3.4 验证函数
```typescript
/**
 * 验证表达式语法是否正确
 * @param expression 用户输入的表达式
 * @returns 验证结果
 */
export function validateExpression(expression: string): ExpressionValidationResult {
  // 空表达式视为有效（表示不启用）
  if (!expression || !expression.trim()) {
    return { valid: true };
  }
  
  try {
    // 尝试转换表达式
    const transformed = transformExpression(expression);
    
    // 尝试解析（会抛出异常如果语法错误）
    parser.parse(transformed);
    
    return { valid: true };
  } catch (error: any) {
    mainLogger.warn('表达式验证失败: {}', error.message);
    return {
      valid: false,
      error: `语法错误: ${error.message}`,
      position: extractErrorPosition(error.message)
    };
  }
}

/**
 * 从错误消息中提取错误位置（如果可能）
 */
function extractErrorPosition(errorMessage: string): number | undefined {
  // expr-eval 的错误消息可能包含位置信息
  const match = errorMessage.match(/position\s+(\d+)/i);
  if (match) {
    return parseInt(match[1]);
  }
  return undefined;
}
```

#### 3.5 评估函数
```typescript
/**
 * 评估表达式是否匹配文本
 * @param expression 用户输入的表达式
 * @param text 待检测文本
 * @returns 评估结果
 */
export function evaluateExpression(
  expression: string,
  text: string
): ExpressionEvaluationResult {
  // 空表达式视为不匹配
  if (!expression || !expression.trim()) {
    return { matched: false };
  }
  
  try {
    // 转换表达式
    const transformed = transformExpression(expression);
    
    // 解析表达式
    const parsed = parser.parse(transformed);
    
    // 评估表达式
    const context: ExpressionContext = {
      text,
      keywords: {}  // 由 contains 函数动态计算
    };
    
    const result = parsed.evaluate(context);
    
    return { matched: Boolean(result) };
  } catch (error: any) {
    mainLogger.error('表达式评估失败: {}', error.message);
    return {
      matched: false,
      error: `评估失败: ${error.message}`
    };
  }
}
```

#### 3.6 缓存机制（可选优化）
```typescript
// 简单的 LRU 缓存
interface CacheEntry {
  transformed: string;
  timestamp: number;
}

const expressionCache = new Map<string, CacheEntry>();
const CACHE_MAX_SIZE = 100;
const CACHE_TTL = 5 * 60 * 1000;  // 5 分钟

/**
 * 获取或计算转换后的表达式（带缓存）
 */
function getTransformedExpression(expression: string): string {
  const now = Date.now();
  const cached = expressionCache.get(expression);
  
  // 检查缓存是否有效
  if (cached && (now - cached.timestamp) < CACHE_TTL) {
    return cached.transformed;
  }
  
  // 计算并缓存
  const transformed = transformExpression(expression);
  
  // 如果缓存已满，删除最旧的条目
  if (expressionCache.size >= CACHE_MAX_SIZE) {
    const oldestKey = Array.from(expressionCache.keys())[0];
    expressionCache.delete(oldestKey);
  }
  
  expressionCache.set(expression, {
    transformed,
    timestamp: now
  });
  
  return transformed;
}
```

**注意事项**：
- ✅ 使用 mainLogger 记录日志
- ✅ 单例 Parser 避免重复创建
- ✅ 完善的错误处理
- ✅ 字符串转义防止注入
- ✅ 可选的缓存机制提升性能

---

### Step 4: 编写单元测试

**文件路径**: `tests/unit/expression-parser.test.ts`

**测试用例**：

```typescript
import { describe, it, expect } from 'vitest';
import { validateExpression, evaluateExpression } from '../../src/utils/expression-parser';

describe('Expression Parser', () => {
  describe('validateExpression', () => {
    it('应该接受空表达式', () => {
      const result = validateExpression('');
      expect(result.valid).toBe(true);
    });
    
    it('应该接受简单关键词', () => {
      const result = validateExpression('密码');
      expect(result.valid).toBe(true);
    });
    
    it('应该接受逻辑与表达式', () => {
      const result = validateExpression('密码 & 身份证');
      expect(result.valid).toBe(true);
    });
    
    it('应该接受逻辑或表达式', () => {
      const result = validateExpression('密码 | 身份证');
      expect(result.valid).toBe(true);
    });
    
    it('应该接受非运算', () => {
      const result = validateExpression('!密码');
      expect(result.valid).toBe(true);
    });
    
    it('应该接受括号分组', () => {
      const result = validateExpression('(密码 | 身份证) & 机密');
      expect(result.valid).toBe(true);
    });
    
    it('应该拒绝括号不匹配的表达式', () => {
      const result = validateExpression('(密码 & 身份证');
      expect(result.valid).toBe(false);
      expect(result.error).toBeDefined();
    });
    
    it('应该拒绝无效的运算符', () => {
      const result = validateExpression('密码 &');
      expect(result.valid).toBe(false);
    });
  });
  
  describe('evaluateExpression', () => {
    it('应该匹配简单关键词', () => {
      const result = evaluateExpression('密码', '这是密码测试');
      expect(result.matched).toBe(true);
    });
    
    it('应该不匹配不存在的关键词', () => {
      const result = evaluateExpression('密码', '这是测试文档');
      expect(result.matched).toBe(false);
    });
    
    it('应该正确评估逻辑与', () => {
      const result = evaluateExpression(
        '密码 & 身份证',
        '密码:123, 身份证:456'
      );
      expect(result.matched).toBe(true);
    });
    
    it('应该在缺少一个关键词时不匹配逻辑与', () => {
      const result = evaluateExpression(
        '密码 & 身份证',
        '只有密码'
      );
      expect(result.matched).toBe(false);
    });
    
    it('应该正确评估逻辑或', () => {
      const result = evaluateExpression(
        '密码 | 身份证',
        '只有密码'
      );
      expect(result.matched).toBe(true);
    });
    
    it('应该正确评估非运算', () => {
      const result = evaluateExpression('!密码', '这是测试文档');
      expect(result.matched).toBe(true);
    });
    
    it('应该在包含关键词时非运算不匹配', () => {
      const result = evaluateExpression('!密码', '这是密码测试');
      expect(result.matched).toBe(false);
    });
    
    it('应该正确评估复杂表达式', () => {
      const result = evaluateExpression(
        '!密码 & (身份证 | 银行卡)',
        '没有密码，有身份证'
      );
      expect(result.matched).toBe(true);
    });
    
    it('应该在第一个条件为假时复杂表达式不匹配', () => {
      const result = evaluateExpression(
        '!密码 & (身份证 | 银行卡)',
        '有密码，有身份证'
      );
      expect(result.matched).toBe(false);
    });
  });
});
```

**运行测试**：
```bash
pnpm test tests/unit/expression-parser.test.ts
```

**预期结果**：所有测试通过 ✅

---

### Step 5: 扩展配置管理

**文件路径**: `src/types/index.ts`

**操作**：在 AppConfig 接口中添加新字段

```typescript
export interface AppConfig {
  // ... 现有字段 ...
  selectedPaths: string[];
  selectedExtensions: string[];
  enabledSensitiveTypes: string[];
  ignoreDirNames: string[];
  systemDirs: string[];
  maxFileSizeMb: number;
  maxPdfSizeMb: number;
  scanConcurrency: number;
  theme: string;
  language: string;
  enableExperimentalParsers: boolean;
  enableOfficeParsers: boolean;
  deleteToTrash: boolean;
  ignoreOtherDrivesSystemDirs: boolean;
  
  // 【新增】自定义敏感词逻辑表达式
  customSensitiveExpression?: string;  // 默认为空字符串（不启用）
}
```

**文件路径**: `src/core/config/manager.ts`

**操作**：在 getDefaultConfig() 中添加默认值

```typescript
export function getDefaultConfig(): AppConfig {
  return {
    // ... 其他默认值 ...
    customSensitiveExpression: ''  // 默认为空，表示不启用自定义表达式
  };
}
```

**验证**：
- ✅ loadConfig() 自动合并新字段（展开运算符）
- ✅ saveConfig() 自动保存新字段（JSON.stringify）
- ✅ 向后兼容：旧配置文件无此字段时使用默认值

---

### Step 6: 修改敏感数据检测函数

**文件路径**: `src/detection/sensitive-detector.ts`

**操作**：扩展现有函数签名

#### 6.1 导入依赖
```typescript
import { evaluateExpression } from '../utils/expression-parser';
import { mainLogger } from '../logger/logger';
```

#### 6.2 新增专用函数（性能优化）

**重要**：为避免重复扫描文本，新增一个只评估自定义表达式的函数：

```typescript
/**
 * 仅评估自定义表达式（不扫描内置规则）
 * 
 * 【性能优化】用于流式处理场景，避免重复扫描文本
 * 
 * @param text 待检测文本
 * @param customExpression 自定义逻辑表达式
 * @returns 是否匹配
 */
export function evaluateCustomExpressionOnly(
  text: string,
  customExpression: string
): boolean {
  if (!customExpression || !customExpression.trim()) {
    return false;
  }
  
  try {
    const result = evaluateExpression(customExpression, text);
    return result.matched;
  } catch (error: any) {
    mainLogger.warn('自定义表达式评估失败: {}', error.message);
    return false;
  }
}
```

**为什么需要这个函数？**
- ❌ `countSensitiveMatches()` 会调用 `detectBuiltinRules()`，导致重复扫描
- ✅ `evaluateCustomExpressionOnly()` 只评估表达式，不重复扫描内置规则
- 性能提升：从 2 次完整扫描降低到 1 次扫描 + 轻量级表达式评估

#### 6.3 修改 detectSensitiveData 函数
```typescript
/**
 * 检测敏感数据（包含自定义表达式）
 * @param text 待检测文本
 * @param enabledTypes 启用的内置规则类型
 * @param customExpression 自定义逻辑表达式（可选）
 * @returns 敏感类型计数
 */
export function detectSensitiveData(
  text: string, 
  enabledTypes: string[],
  customExpression?: string  // 新增参数
): Record<string, number> {
  // 1. 执行内置规则检测
  const counts = detectBuiltinRules(text, enabledTypes);
  
  // 2. 执行自定义表达式检测（如果提供且非空）
  if (customExpression && customExpression.trim()) {
    try {
      const result = evaluateExpression(customExpression, text);
      if (result.matched) {
        counts['custom_expression'] = 1;
      }
    } catch (error: any) {
      // 表达式评估失败，记录警告但不影响其他检测
      mainLogger.warn('自定义表达式评估失败: {}', error.message);
    }
  }
  
  return counts;
}

/**
 * 提取内置规则检测逻辑为独立函数
 */
function detectBuiltinRules(text: string, enabledTypes: string[]): Record<string, number> {
  const counts: Record<string, number> = {};
  
  for (const rule of sensitiveRules) {
    if (!enabledTypes.includes(rule.id)) continue;
    
    const pattern = new RegExp(rule.pattern.source, rule.pattern.flags);
    const matches = Array.from(text.matchAll(pattern));
    if (!matches || matches.length === 0) continue;
    
    let validCount = 0;
    for (const match of matches) {
      if (rule.validate) {
        if (rule.validate(match[0], text, match.index!)) {
          validCount++;
        }
      } else {
        validCount++;
      }
    }
    
    if (validCount > 0) {
      counts[rule.id] = validCount;
    }
  }
  
  return counts;
}
```

#### 6.4 修改 countSensitiveMatches 函数（扫描模式专用）
```typescript
export function countSensitiveMatches(
  text: string, 
  enabledTypes: string[],
  customExpression?: string  // 新增参数
): Record<string, number> {
  // 实现逻辑与 detectSensitiveData 类似
  const counts = detectBuiltinRules(text, enabledTypes);
  
  if (customExpression && customExpression.trim()) {
    try {
      const result = evaluateExpression(customExpression, text);
      if (result.matched) {
        counts['custom_expression'] = 1;
      }
    } catch (error: any) {
      mainLogger.warn('自定义表达式评估失败: {}', error.message);
    }
  }
  
  return counts;
}
```

**注意事项**：
- ✅ 保持向后兼容：customExpression 为可选参数
- ✅ 错误不影响其他检测：try-catch 包裹
- ✅ 使用 mainLogger 记录警告

---

### Step 7: 集成到 Worker 线程

**文件路径**: `src/workers/file-worker.ts`

**操作**：接收并传递 customExpression 参数

#### 7.1 修改 WorkerTask 类型
```typescript
interface WorkerTask {
  taskId: number;
  filePath: string;
  enabledSensitiveTypes: string[];
  customExpression?: string;  // 新增字段
  previewMode?: boolean;
  streamMode?: boolean;
  chunkSize?: number;
  config?: {
    maxFileSizeMb: number;
    maxPdfSizeMb: number;
  };
}
```

#### 7.2 修改 processTask 函数
```typescript
async function processTask(task: WorkerTask): Promise<void> {
  const {
    taskId,
    filePath,
    enabledSensitiveTypes,
    customExpression,  // 解构新字段
    previewMode,
    streamMode,
    chunkSize,
    config
  } = task;
  
  try {
    // 读取文件内容
    const content = await readFileContent(filePath);
    
    // 执行敏感数据检测（传入 customExpression）
    const sensitiveCounts = detectSensitiveData(
      content,
      enabledSensitiveTypes,
      customExpression  // 传递自定义表达式
    );
    
    // 返回结果
    parentPort?.postMessage({
      type: 'result',
      taskId,
      sensitiveCounts,
      // ... 其他字段 ...
    });
  } catch (error: any) {
    parentPort?.postMessage({
      type: 'error',
      taskId,
      error: error.message
    });
  }
}
```

---

### Step 8: 添加 IPC Handlers

**文件路径**: `src/core/main/ipc-handlers.ts`

**操作**：注册 3 个新的 IPC handlers

#### 8.1 导入依赖
```typescript
import { validateExpression } from '../../utils/expression-parser';
import { ScanState } from '../state/scan-state';
```

#### 8.2 注册 handlers
```typescript
export function setupIpcHandlers(
  getMainWindow: () => BrowserWindow | null,
  scanState: ScanState,
  powerSaveManager: PowerSaveManager,
  previewWorkerManager: PreviewWorkerManager
): void {
  // ... 现有 handlers ...
  
  // 【新增】设置自定义表达式
  ipcMain.handle('set-custom-expression', async (_, expression: string) => {
    try {
      // 验证表达式语法
      if (expression && expression.trim()) {
        const validation = validateExpression(expression);
        if (!validation.valid) {
          return { 
            success: false, 
            error: validation.error 
          };
        }
      }
      
      // 保存到配置
      const config = await loadConfig();
      config.customSensitiveExpression = expression;
      await saveConfig(config);
      
      mainLogger.info('自定义表达式已保存');
      
      return { success: true };
    } catch (error: any) {
      mainLogger.error('保存自定义表达式失败: {}', error.message);
      return { 
        success: false, 
        error: `保存失败: ${error.message}` 
      };
    }
  });
  
  // 【新增】获取当前自定义表达式
  ipcMain.handle('get-custom-expression', async () => {
    try {
      const config = await loadConfig();
      return { 
        success: true, 
        expression: config.customSensitiveExpression || '' 
      };
    } catch (error: any) {
      mainLogger.error('获取自定义表达式失败: {}', error.message);
      return { 
        success: false, 
        error: error.message 
      };
    }
  });
  
  // 【新增】验证表达式语法（用于前端实时校验）
  ipcMain.handle('validate-expression', (_, expression: string) => {
    const result = validateExpression(expression);
    return result;
  });
}
```

---

### Step 9: 暴露 Preload API

**文件路径**: `src/preload.ts`

**操作**：添加新的 API 暴露

```typescript
contextBridge.exposeInMainWorld('api', {
  // ... 现有 API ...
  
  // 【新增】自定义表达式相关
  setCustomExpression: (expression: string) => 
    ipcRenderer.invoke('set-custom-expression', expression),
  getCustomExpression: () => 
    ipcRenderer.invoke('get-custom-expression'),
  validateExpression: (expression: string) => 
    ipcRenderer.invoke('validate-expression', expression)
});
```

---

### Step 10: 添加 TypeScript 类型声明

**文件路径**: `frontend/src/types/electron.d.ts`

**操作**：添加新的 API 类型声明

```typescript
interface Window {
  api: {
    // ... 现有 API ...
    
    // 【新增】自定义表达式相关
    setCustomExpression: (expression: string) => Promise<{ 
      success: boolean; 
      error?: string 
    }>;
    getCustomExpression: () => Promise<{ 
      success: boolean; 
      expression?: string; 
      error?: string 
    }>;
    validateExpression: (expression: string) => Promise<{ 
      valid: boolean; 
      error?: string; 
      position?: number 
    }>;
  };
}
```

---

### Step 11: 添加工具栏输入框 UI

**文件路径**: `frontend/src/components/Toolbar.vue`

**操作**：在工具栏右侧添加输入框

#### 11.1 Template
```vue
<template>
  <div class="toolbar">
    <!-- ... 现有工具栏元素 ... -->
    
    <!-- 【新增】自定义表达式输入框 -->
    <div class="custom-expression-input">
      <input 
        v-model="expression" 
        @input="onInput"
        :class="{ 'error': validationError, 'valid': isValid }"
        placeholder="输入敏感词（支持 !、()、&、|）"
      />
      <div v-if="validationError" class="error-message">
        {{ validationError }}
      </div>
      <div v-if="isValid && expression" class="valid-indicator">
        ✓
      </div>
    </div>
  </div>
</template>
```

#### 11.2 Script
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue';

const expression = ref('');
const validationError = ref<string>('');
const isValid = ref(false);
let debounceTimer: NodeJS.Timeout | null = null;

// 防抖验证
const onInput = () => {
  if (debounceTimer) clearTimeout(debounceTimer);
  
  debounceTimer = setTimeout(async () => {
    const result = await window.api.validateExpression(expression.value);
    
    if (result.valid) {
      validationError.value = '';
      isValid.value = true;
      
      // 自动保存有效表达式
      const saveResult = await window.api.setCustomExpression(expression.value);
      if (!saveResult.success) {
        validationError.value = saveResult.error || '保存失败';
        isValid.value = false;
      }
    } else {
      validationError.value = result.error || '语法错误';
      isValid.value = false;
    }
  }, 300);  // 300ms 防抖
};

// 加载已保存的表达式
onMounted(async () => {
  const result = await window.api.getCustomExpression();
  if (result.success) {
    expression.value = result.expression || '';
    if (expression.value) {
      isValid.value = true;
    }
  }
});
</script>
```

#### 11.3 Style
```vue
<style scoped>
.custom-expression-input {
  position: relative;
  margin-left: 10px;
}

.custom-expression-input input {
  padding: 5px 10px;
  border: 1px solid #ccc;
  border-radius: 4px;
  width: 250px;
  transition: border-color 0.3s;
}

.custom-expression-input input.valid {
  border-color: #4CAF50;
}

.custom-expression-input input.error {
  border-color: #f44336;
}

.error-message {
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: #ffebee;
  color: #c62828;
  padding: 5px;
  border-radius: 4px;
  font-size: 12px;
  z-index: 1000;
}

.valid-indicator {
  position: absolute;
  right: 10px;
  top: 50%;
  transform: translateY(-50%);
  color: #4CAF50;
  font-weight: bold;
}
</style>
```

---

### Step 12: 在扫描时传递 customExpression

**文件路径**: `src/core/scanner.ts` 或相关文件

**操作**：在启动扫描时获取并传递 customExpression

```typescript
// 在 startScan 函数中
const config = await loadConfig();
const customExpression = config.customSensitiveExpression;

// 创建任务时传递
const task: WorkerTask = {
  taskId,
  filePath,
  enabledSensitiveTypes: config.enabledSensitiveTypes,
  customExpression,  // 传递自定义表达式
  // ... 其他字段 ...
};
```

---

## ✅ 验收清单

### 功能验收
- [ ] 输入框在工具栏右侧正确显示
- [ ] 支持所有逻辑运算符（`!`、`&`、`|`、`()`）
- [ ] 优先级规则正确执行
- [ ] 语法错误有清晰的提示
- [ ] 表达式能正确匹配文件内容
- [ ] 与现有敏感词规则兼容
- [ ] 配置持久化：重启应用后表达式保留

### 性能验收
- [ ] 大文件扫描不明显变慢（增加 < 30%）
- [ ] 表达式缓存生效
- [ ] 短路求值生效

### 代码质量验收
- [ ] 无 TypeScript 编译错误
- [ ] 无 ESLint 警告
- [ ] 所有单元测试通过
- [ ] 日志使用 mainLogger
- [ ] 无内存泄漏风险

### 文档验收
- [ ] 实施文档完整
- [ ] 代码注释清晰
- [ ] 关键决策有说明

---

## 🚀 开始实施

按照上述步骤逐一实施，每完成一个 Step 就提交一次代码。

**建议提交顺序**：
1. Step 1-4: 基础架构 + 测试
2. Step 5-8: 后端集成
3. Step 9-11: 前端集成
4. Step 12: 扫描流程集成
5. 最终测试和优化

---

**下一步**：开始 Step 1 - 安装依赖库
